<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itmal.auth.repository.UserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (email, password, nickname, native_language, role, email_verified)
        VALUES (#{email}, #{password}, #{nickname}, #{nativeLanguage}, #{role}, #{emailVerified})
    </insert>

    <select id="findByEmail" resultType="com.itmal.auth.domain.User">
        SELECT *
        FROM users
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM users
        WHERE email = #{email}
    </select>

    <select id="existsByNickname" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM users
        WHERE nickname = #{nickname}
    </select>

</mapper>
