<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itmal.mypage.mapper.MypageMapper">

    <select id="countQuestionByUserId" resultType="int">
        SELECT COUNT(*) FROM question WHERE user_id = #{userId} AND deleted_at IS NULL
    </select>

    <select id="countAnswerByUserId" resultType="int">
        SELECT COUNT(*) FROM answer WHERE user_id = #{userId} AND deleted_at IS NULL
    </select>

    <select id="countCommentByUserId" resultType="int">
        SELECT COUNT(*) FROM comment WHERE user_id = #{userId} AND deleted_at IS NULL
    </select>

</mapper>
