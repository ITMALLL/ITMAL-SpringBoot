<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itmal.chat.mapper.ChatMessageMapper">

    <!-- 메시지 저장 -->
    <insert id="insertChatMessage" parameterType="ChatMessageDto">
        INSERT INTO chat_message (chat_room_id, sender_id, content, is_read, created_at)
        VALUES (#{chatRoomId}, #{senderId}, #{content}, #{isRead}, #{createdAt})
    </insert>

    <!-- 채팅방의 메시지 목록 조회 -->
    <select id="selectByChatRoomId" parameterType="Long" resultType="ChatMessageDto">
        SELECT * FROM chat_message
        WHERE chat_room_id = #{chatRoomId}
        ORDER BY created_at ASC
    </select>

</mapper>